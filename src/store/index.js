import { createStore } from 'vuex'
import uInfo from './state/userinfo.stata.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    uInfo
  }
})
